## Packages
framer-motion | Smooth animations for page transitions and interactions
react-hook-form | Form state management
@hookform/resolvers | Zod resolver for react-hook-form
date-fns | Date formatting for the admin table

## Notes
- Tailwind config should include "Space Grotesk" for display font and "Inter" for body
- Replit Auth is already set up; using useAuth hook for admin protection
- Offers API endpoints are defined in shared/routes
